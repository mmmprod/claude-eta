## Build
node_modules/
*.tsbuildinfo
# NOTE: dist/ is intentionally committed.
# Claude Code plugins are installed from a git checkout with no post-install build.
# If dist/ is missing or stale, runtime hooks and /eta break for users.

## Secrets & env
.env*
*.pem
*.key

## Plugin data (contains personal task history — never push)
data/
_active.json

## IDE & editor
.vscode/
.idea/
*.swp
*.swo
*~

## Claude Code local state (personal, per-machine)
.claude/
.agents/

## Supabase local temp state
supabase/.temp/

## Cursor local state
.cursor/

## BMAD local output & config (personal brainstorming, project-specific)
_bmad/
_bmad-output/

## OS
.DS_Store
Thumbs.db
